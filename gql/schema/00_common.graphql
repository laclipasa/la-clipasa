# for vscode gql extension
type Query {
  _empty: String
}

# for vscode gql extension
type Mutation {
  _empty: String
}

scalar Cursor

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: Cursor
  endCursor: Cursor
}

interface Node {
  nodeId: ID!
}

scalar Time

